---
phase: iteration-2
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-02-27
---

<current_state>
App is fully built and running on http://localhost:3000 on branch `feat/frontend`.
All 5 pages compile and render (dashboard, demand, heatmap, simulator, stand detail).
API endpoints work. E2E Gherkin/Cucumber tests written.
Iteration 2 plan is approved but NOT yet started.
</current_state>

<completed_work>
- Next.js 14 project scaffolded with TypeScript, Tailwind, Recharts
- Types, mock data, calculations, constants, GameContext all working
- Root layout with Sidebar + GameSelector
- Dashboard page with KPI cards, bottleneck list, stress windows, assumptions
- Demand Curve page with Recharts line chart, per-stand toggles, game event markers
- Heatmap page with custom Tailwind grid, color-coded cells, clickable stands
- Simulator page with editable staff counts, debounced recalc, recommended moves
- Stand Detail page with per-bucket table
- Mock API routes: /api/games, /api/forecast/:id, /api/simulate/:id, /api/live-status/:id
- E2E tests: 4 Gherkin feature files + Cucumber step definitions + Playwright world
- Revenue at risk tuned to ~$3,795 with clear bottlenecks
- Codex CLI installed and logged in
- GSD framework installed globally
</completed_work>

<remaining_work>
### Iteration 2 (4 tasks, approved plan at /Users/nimbleprofit/.claude/plans/eventual-foraging-dahl.md):

1. **Tooltip info icons on KPI cards** — PARTIALLY DONE
   - KPICard.tsx updated with `tooltip` prop + hover popover (COMMITTED)
   - REMAINING: Add `tooltip="..."` to all KPICard usages in:
     - `src/app/dashboard/page.tsx` (4 cards)
     - `src/app/simulator/page.tsx` (4 cards)
     - `src/app/stands/[standId]/page.tsx` (5 cards)

2. **AI Insights component** — Mock AI panel with typing animation on dashboard + simulator
   - Create: `src/app/api/insights/[gameId]/route.ts`
   - Create: `src/components/shared/AIInsights.tsx`
   - Modify: `src/app/dashboard/page.tsx`, `src/app/simulator/page.tsx`
   - Data-driven mock insights (not random), typing animation for demo wow

3. **Replace mock data with REAL arena data** — Victoria Royals, Save-On-Foods Memorial Centre
   - Source: `/Users/nimbleprofit/Downloads/data from arena - save on foods memorial center/`
   - GameDetails.xlsx: columns = Event(opponent), Day, Date, Puck Drop, Attendance
   - 14 CSV files: columns = Date, Time, Category, Item, Qty, Price Point Name, Location
   - Real stands like "SOFMC ReMax Fan Deck"
   - Parse with python3+openpyxl (already installed) or node script
   - Replace MOCK_GAMES and MOCK_STANDS with real data
   - Derive demand curves from actual transaction timestamps

4. **Timestamps and execution timer** — Show real timestamps, "Data processed in X.XXms"
   - Add execution timer to dashboard
   - Add "Last Updated" to panels
   - Live mode: show elapsed time prominently
</remaining_work>

<decisions_made>
- Next.js 14 (App Router) + Tailwind + Recharts — user chose these
- Dark theme ("control room" aesthetic) — bg-gray-950 base
- React Context for state (no Redux/Zustand) — sufficient for this app
- Mock AI for demo — user doesn't have API key, will swap to real Claude API post-hackathon
- Cut Live Mode page (Page G) — time constraint
- Kept Stand Detail page (simplified)
- Demand multiplier set to 1.4x capacity for realistic revenue-at-risk numbers
- Branch strategy: feat/frontend (user) + teammate's branch, PRs to main
- CLAUDE.md on main with workflow rules
</decisions_made>

<blockers>
- None currently
</blockers>

<context>
This is a hackathon project for VTN26 at Save-On-Foods Memorial Centre (Victoria Royals WHL).
Teammate (mansahaj on GitHub) handles backend.
User is on `feat/frontend` branch of `mansahaj/vtn` repo.
User wants real historical data from the arena integrated.
The "AI component" question is critical — judges will ask "where's the AI?"
The typing animation mock is the pragmatic path given no API key.
Dev server runs on port 3000.
</context>

<next_action>
Start with: Run `/gsd:resume-work` then execute iteration 2 tasks in order:
1. KPICard tooltips (quickest win, ~15 min)
2. Real data integration (biggest impact, ~45 min — parse xlsx+csv, replace mock-data.ts)
3. AI Insights mock component (innovation score, ~30 min)
4. Timestamps/timer (polish, ~15 min)
</next_action>
